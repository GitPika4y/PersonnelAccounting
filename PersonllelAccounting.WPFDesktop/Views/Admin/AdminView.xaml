<UserControl x:Class="WPF_Desktop.Views.Admin.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WPF_Desktop.Views.Admin"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid Background="AntiqueWhite">
        <Grid.ColumnDefinitions>
            <!-- MARGIN_AUTO -->
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="2*" MinWidth="250"/>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="6*" MinWidth="750"/>
            <!-- MARGIN_AUTO -->
            <ColumnDefinition Width="0.4*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>

        <md:Card Grid.Column="1"
                 Grid.Row="1"
                 Background="Moccasin"
                 >
            <local:AdminSideBarView Content="{Binding SideBarViewModel}"/>
        </md:Card>

        <md:Card Grid.Column="3"
                 Grid.Row="1">
            <ContentControl Content="{Binding SideBarViewModel.NavigationService.CurrentViewModel}"/>
        </md:Card>
    </Grid>
</UserControl>
