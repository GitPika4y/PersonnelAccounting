<UserControl x:Class="WPF_Desktop.Views.User.Partials.EmployeePassportAddEditPartial"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Column="0"
                 Grid.Row="0"
                 Text="{Binding PassportSerial, UpdateSourceTrigger=PropertyChanged}"
                 md:HintAssist.Hint="Серия"
                 Margin="20 20 20 0"/>
        <TextBox Grid.Column="1"
                 Grid.Row="0"
                 Text="{Binding PassportNumber, UpdateSourceTrigger=PropertyChanged}"
                 md:HintAssist.Hint="Номер"
                 Margin="20 20 20 0"/>

        <StackPanel Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Grid.Row="1"
                    Margin="0 20">
            <TextBox Text="{Binding PassportGivenBy, UpdateSourceTrigger=PropertyChanged}"
                     md:HintAssist.Hint="Кем выдан"
                     Margin="20 0"/>

            <DatePicker SelectedDate="{Binding PassportDate}"
                        Margin="20 20 20 0"
                        md:HintAssist.Hint="Дата выдачи"/>
        </StackPanel>
    </Grid>
</UserControl>
