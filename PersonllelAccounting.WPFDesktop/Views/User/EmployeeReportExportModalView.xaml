<UserControl x:Class="WPF_Desktop.Views.User.EmployeeReportExportModalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">

	<StackPanel Margin="10">
		<TextBlock Text="Составить отчет по сотрудникам"
		           Style="{StaticResource MaterialDesignHeadlineSmallTextBlock}"
		           HorizontalAlignment="Center"
		           Margin="0 0 0 10"/>

		<TextBlock Text="{Binding Count,
					StringFormat={}Записей: {0}}"
		           Style="{StaticResource MaterialDesignTitleMediumTextBlock}"/>

		<TextBlock Text="{Binding FiltersDescription}"
		           Style="{StaticResource MaterialDesignTitleMediumTextBlock}"
		           Visibility="{Binding FiltersDescription,
					Converter={StaticResource NotNullToVisibilityConverter}}"/>

		<DataGrid x:Name="EmployeeGrid"
		          MaxHeight="400"
		          MaxWidth="1000"
		          ItemsSource="{Binding Employees}"
		          IsReadOnly="True"/>

		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>

			<Button Grid.Column="0"
			        Content="Сохранить"
			        Command="{Binding SaveCommand}"
			        Margin="0 0 5 0"/>
			<Button Grid.Column="1"
			        Content="Назад"
			        Style="{StaticResource MaterialDesignOutlinedSecondaryButton}"
			        Command="{x:Static md:DialogHost.CloseDialogCommand}"
			        Margin="5 0 0 0"/>
		</Grid>
	</StackPanel>
</UserControl>
