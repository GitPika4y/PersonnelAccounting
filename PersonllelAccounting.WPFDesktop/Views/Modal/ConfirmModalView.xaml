<UserControl x:Class="WPF_Desktop.Views.Modal.ConfirmModalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"

             >

    <StackPanel Margin="20 15"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MaxWidth="600"
                MaxHeight="800">

	    <TextBlock Text="{Binding Title}"
	               Style="{StaticResource MaterialDesignDisplaySmallTextBlock}"
	               Margin="0 10"
	               HorizontalAlignment="Center"/>

	    <ScrollViewer VerticalScrollBarVisibility="Auto"
	                  MaxHeight="500">
		    <TextBlock Text="{Binding Content}"
		               Style="{StaticResource MaterialDesignBodyLargeTextBlock}"
		               Margin="0 10"
		               TextWrapping="Wrap"
		    />
	    </ScrollViewer>

        <Grid Margin="20"
              IsSharedSizeScope="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition SharedSizeGroup="col"/>
                <ColumnDefinition SharedSizeGroup="col"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="{Binding ConfirmButtonText}"
                    Command="{Binding CloseCommand}"
                    Margin="0 0 5 0"
            />

            <Button Grid.Column="1"
                    Content="{Binding DenyButtonText}"
                    Command="{x:Static md:DialogHost.CloseDialogCommand}"
                    Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                    Margin="5 0 0 0"
            />
        </Grid>
    </StackPanel>
</UserControl>
